DATABASE_URL=postgresql://postgres:postgres@localhost:5432/xuanshang
ADMIN_EMAILS=
# Generate with: openssl rand -base64 32
APP_MASTER_KEY=replace-with-base64-encoded-32-byte-key
# Production Compose injects these internal relay values automatically.
SMTP_HOST=localhost
SMTP_PORT=2525
SMTP_USERNAME=wasp
SMTP_PASSWORD=development-relay-password
